# Walkthrough - Chart Interaction Lock

I have updated the app to lock the main chart, preventing accidental zooming or panning.

## Changes

### [app.py](file:///c:/Users/Tomi/.gemini/app.py)

#### [MODIFY] [app.py](file:///c:/Users/Tomi/.gemini/app.py)
-   Updated `fig.update_layout`:
    -   Set `dragmode=False`.
    -   Set `xaxis` and `yaxis` to `fixedrange=True`.
-   Updated `st.plotly_chart`:
    -   Set `config={'scrollZoom': False, 'displayModeBar': False}`.

## Verification Results

### Manual Verification
-   **Interaction**: Try to drag the chart with the mouse. It should NOT move.
-   **Zoom**: Try to scroll with the mouse wheel over the chart. It should NOT zoom.
-   **Toolbar**: The Plotly toolbar (top right of the chart) should be hidden.
-   **Initial View**: The chart should still automatically focus on the relevant recent data when loaded.

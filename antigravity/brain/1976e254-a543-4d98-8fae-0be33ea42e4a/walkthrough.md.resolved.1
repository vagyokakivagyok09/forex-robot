# Walkthrough - Logic Fix: Disable Trading Plan on Weak Matches

I have updated the app to prevent generating misleading trading plans when the pattern matching confidence is low.

## Changes

### [app.py](file:///c:/Users/Tomi/.gemini/app.py)

#### [MODIFY] [app.py](file:///c:/Users/Tomi/.gemini/app.py)
-   Added a check `if match_count > 0:` before plotting the Ghost Cloud and Average Projection.
-   Added the same check before generating the "Trading Plan Analysis".
-   If `match_count == 0`, the app now hides the plan and displays a warning: "⚠️ **Nincs elég erős minta.** ..."

## Verification Results

### Manual Verification
-   **Scenario A (Strong Match)**: If `Bizalmi Index` > 0 (e.g., "Magas" or "Közepes"), the Ghost Cloud and Trading Plan should appear as normal.
-   **Scenario B (No Match)**: If `Bizalmi Index` is 0 ("Alacsony" with 0 matches > 0.80), the chart should NOT show the orange projection line, and the Trading Plan section should be replaced by the warning message.

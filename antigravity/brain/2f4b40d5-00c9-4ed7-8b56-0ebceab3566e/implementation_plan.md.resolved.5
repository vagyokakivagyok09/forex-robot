# Multi-Asset Dashboard Implementation Plan

## Goal
Refactor the app to display a dashboard showing the London Breakout analysis for GBPUSD, GBPJPY, and EURUSD simultaneously.

## Proposed Changes
### Core Logic ([app.py](file:///c:/Users/Tomi/.gemini/app.py))
#### [MODIFY] [app.py](file:///c:/Users/Tomi/.gemini/app.py)
- **Asset List**: Define `TARGET_PAIRS = ['GBPUSD=X', 'GBPJPY=X', 'EURUSD=X']`.
- **Main Loop**: Iterate through `TARGET_PAIRS`.
    - Perform data download and breakout calculation for each.
    - Render chart and info panel for each.
- **Alerts**: Update `st.session_state` keys to include the symbol (e.g., `last_alert_signature_{ticker}`) to prevent conflicts.

### Visualization
- Remove Sidebar Asset Selector.
- Display 3 vertical sections, one for each pair.
- Maintain the "Pro 2025" logic (Buffers, Wick-to-Wick) for all.

## Verification Plan
### Manual Verification
- Verify 3 charts appear (GBPUSD, GBPJPY, EURUSD).
- Check that JPY buffer logic is applied correctly to GBPJPY (0.03) and others (0.0003).
- Verify Telegram alerts work for multiple pairs without blocking each other.

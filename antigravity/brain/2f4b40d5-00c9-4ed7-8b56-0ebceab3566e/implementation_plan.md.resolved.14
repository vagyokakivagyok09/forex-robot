# Hougaard Dashboard Implementation Plan

## Goal
Create a dashboard displaying 3 simultaneous charts for GBPUSD, GBPJPY, and EURUSD, implementing the London Daybreak strategy with strict directional projections based on EMA 50.

## Proposed Changes
### Core Logic ([app.py](file:///c:/Users/Tomi/.gemini/app.py))
#### [MODIFY] [app.py](file:///c:/Users/Tomi/.gemini/app.py)
- **Configuration**:
    - `TARGET_PAIRS = ['GBPUSD=X', 'GBPJPY=X', 'EURUSD=X']`.
- **Main Loop**:
    - Remove sidebar selection.
    - Iterate `for ticker in TARGET_PAIRS:`
        - `res = analyze_pair(ticker)`
        - [render_view(ticker, res)](file:///c:/Users/Tomi/.gemini/app.py#201-306) (Render directly in main column, stacked).
- **Visualization ([render_view](file:///c:/Users/Tomi/.gemini/app.py#201-306))**:
    - **Trend Filter**: Check `res['trend']`.
    - **Projection**:
        - If BULLISH: Draw GREEN dashed line from `Long Entry` to `Target`. Hide Short levels.
        - If BEARISH: Draw RED dashed line from `Short Entry` to `Target`. Hide Long levels.
    - **Chart**: Keep EMA 50 (Yellow) and Box (Blue).

## Verification Plan
### Manual Verification
- Verify 3 charts appear vertically.
- Verify each chart shows ONLY the projection matching the EMA 50 trend.
- Verify alerts still work (One Bullet rule).

# London Daybreak Strategy Implementation Plan

## Goal
Refactor the application to implement Tom Hougaard's "London Daybreak" strategy, focusing on the 07:00-08:00 GMT range and using an EMA 50 trend filter.

## Proposed Changes
### Core Logic ([app.py](file:///c:/Users/Tomi/.gemini/app.py))
#### [MODIFY] [app.py](file:///c:/Users/Tomi/.gemini/app.py)
- **Configuration**:
    - Update `TARGET_PAIRS` to `['GBPUSD=X', 'EURUSD=X', 'GBPJPY=X', 'EURGBP=X']`.
- **[analyze_pair](file:///c:/Users/Tomi/.gemini/app.py#86-219) function**:
    - **EMA 50**: Calculate `data['EMA50'] = data['Close'].ewm(span=50).mean()`.
    - **Trend**: Determine if `Close[-1] > EMA50[-1]` (Bullish) or `Close[-1] < EMA50[-1]` (Bearish).
    - **Daybreak Range**: Filter data for `07:00 <= hour < 08:00` (assuming data is UTC/GMT aligned or handled consistently).
    - **Entry Rules**:
        - If Bullish: Only check for `Close > Range_High + Buffer`. Ignore shorts.
        - If Bearish: Only check for `Close < Range_Low - Buffer`. Ignore longs.
    - **One Bullet**: Keep existing logic.

### Visualization
- **Chart**:
    - Add Yellow Line for EMA 50.
    - Draw Box only for the 07:00-08:00 period.
- **Telegram**:
    - Update message to include "Hougaard Daybreak" and Trend status.

## Verification Plan
### Manual Verification
- Check if EMA 50 is plotted correctly.
- Verify the box covers only 1 hour (4 candles).
- Verify that a breakout *against* the trend is IGNORED (no alert).
- Verify that a breakout *with* the trend triggers an alert.

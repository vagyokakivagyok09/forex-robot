# London Breakout Pro 2025 Implementation Plan

## Goal
Upgrade the app to the "Pro 2025" version of the London Breakout strategy, incorporating buffer zones for entries and detailed risk calculations.

## Proposed Changes
### Core Logic ([app.py](file:///c:/Users/Tomi/.gemini/app.py))
#### [MODIFY] [app.py](file:///c:/Users/Tomi/.gemini/app.py)
- **Buffer Logic**:
    - Define `BUFFER_STD = 0.0003` (3 pips).
    - Define `BUFFER_JPY = 0.03` (3 pips for JPY).
    - Select buffer based on ticker symbol.
- **Entry Calculation**:
    - `Long Entry = Asian High + Buffer`
    - `Short Entry = Asian Low - Buffer`
- **Target Calculation**:
    - `Long Target = Long Entry + Box Height` (Maintain 1:1 from Entry)
    - `Short Target = Short Entry - Box Height`

### Visualization
- Update the dashed Entry lines to be drawn at the *Buffered* levels, not the Box edges.
- Keep the Blue Box at the exact High/Low (Wick-to-Wick).

### Alerts
- **Telegram**:
    - Add "📏 Stop Távolság: {pips} pip" line.
    - Calculate pips: [(Entry - SL) * 10000](file:///c:/Users/Tomi/.gemini/app.py#69-300) (or 100 for JPY).

## Verification Plan
### Manual Verification
- Check EURUSD chart: Entry line should be slightly above the blue box.
- Check USDJPY chart: Entry line should be slightly above the blue box (correct scaling).
- Verify Telegram message contains the Pip distance.

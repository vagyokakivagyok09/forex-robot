# Background Monitoring & Single View Implementation Plan

## Goal
Refactor the app to monitor 5 currency pairs in the background for Telegram alerts while providing a focused, single-chart user interface for a selected pair.

## Proposed Changes
### Core Logic ([app.py](file:///c:/Users/Tomi/.gemini/app.py))
#### [MODIFY] [app.py](file:///c:/Users/Tomi/.gemini/app.py)
- **Constants**:
    - Update `TARGET_PAIRS` to `['GBPUSD=X', 'GBPJPY=X', 'EURUSD=X', 'EURJPY=X', 'USDCHF=X']`.
    - Create `STRATEGY_INFO` dictionary with user-provided descriptions.
- **Refactoring**:
    - Split [analyze_and_plot](file:///c:/Users/Tomi/.gemini/app.py#70-311) into:
        - `analyze_pair(ticker)`: Downloads data, calculates logic, sends alerts (Background). Returns analysis results.
        - `render_view(results)`: Draws chart and info panel (Foreground).
- **Main Loop**:
    - Sidebar: Select `selected_pair`.
    - Loop through `TARGET_PAIRS`:
        - Call `analyze_pair(ticker)`.
        - If `ticker == selected_pair`, store results for rendering.
    - Call `render_view(stored_results)`.

### Visualization
- **Sidebar**: Add `selectbox` for "Vizsgált Eszköz".
- **Main Area**:
    - Show "ℹ️ Stratégia Tipp: [Text]" at the top.
    - Render the interactive chart and trading plan for the selected pair only.

## Verification Plan
### Manual Verification
- Select different pairs in the sidebar and verify the chart updates.
- Verify the correct "Strategy Tip" is displayed for each pair.
- (Implicit) Verify Telegram alerts are sent for *any* pair if a breakout occurs (harder to test without live market movement, but logic can be reviewed).
